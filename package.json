{
  "name": "url-shortener",
  "version": "0.1.0",
  "private": true,
  "description": "URL shortener with click stats — Vercel + Postgres",
  "scripts": {
    "build": "echo no-build",
    "test": "node --test",
    "db:migrate": "node --env-file=.env.local db/migrate.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "1.1.0"
  }
}
